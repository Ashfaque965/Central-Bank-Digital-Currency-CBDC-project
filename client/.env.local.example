# Base URL of the CBDC backend API
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# When "true", the app serves mock data and works without a running backend.
# Useful for local UI development and demos.
NEXT_PUBLIC_DEMO_MODE=true
